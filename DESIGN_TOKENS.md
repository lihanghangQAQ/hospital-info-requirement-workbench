# 设计令牌（Design Tokens）· 中国风 / 年轻化 / 圆润

> 来源：v2 重构规范。所有令牌定义在 `index.html` `<style>` 顶部 `:root{}`，全站引用变量，**不写死色值**。

## 一、色板（Color）

| 令牌 | 值 | 语义 | 使用场景 |
|---|---|---|---|
| `--bgPaper` | `#F7F4EE` | 宣纸米 | 页面背景 |
| `--cardWarm` | `#FFFDF8` | 卡片暖白 | 卡片 / 抽屉 / 弹层底 |
| `--inkDeep` | `#23405E` | 主标题墨青 | 页面主标题、品牌字 |
| `--inkText` | `#3d4c5c` | 正文墨灰 | 正文文字 |
| `--inkSub` | `#6a7583` | 次级文字 | 副标题、说明 |
| `--inkFaint` | `#9aa5b1` | 占位/禁用 | 占位符、禁用态 |
| `--primary` | `#3E6E9E` | 靛青（主色）| 主按钮、信息需求标识、导航高亮 |
| `--primary-d` | `#2f5480` | 靛青深 | 主色 hover / 渐变深端 |
| `--primary-l` | `#e8eef5` | 靛青浅 | 主色浅底（选中行、标签底）|
| `--success` | `#4C9A6A` | 竹绿 | 已完成、收入、正向指标 |
| `--success-l` | `#e6f2ea` | 竹绿浅 | 成功标签底 |
| `--warn` | `#C9973B` | 缃金 | 预警、跨年长尾、今日到期 |
| `--warn-l` | `#f7eed9` | 缃金浅 | 预警标签底 |
| `--danger` | `#C4483C` | 朱砂 | 逾期、删除、危险操作 |
| `--danger-l` | `#fae9e7` | 朱砂浅 | 危险标签底 |
| `--violet` | `#7B5EA7` | 黛紫 | 优先级标识、数据统计类型 |
| `--violet-l` | `#eee8f5` | 黛紫浅 | 优先级标签底 |
| `--line` | `#e7e0d4` | 发丝线 | 卡片边框、分割线 |
| `--line-strong` | `#d8d0c2` | 强分割线 | 表头底线、分组线 |

**类型色**：`EN.typeColor = { RQ:"#3E6E9E"(靛青), DS:"#7B5EA7"(黛紫) }`
**状态点（看板列）**：待办 `#8a97a0` · 进行中 `#3E6E9E` · 已完成 `#4C9A6A` · 已关闭 `#9aa5b1`

## 二、圆角（Radius）

| 令牌 | 值 | 使用 |
|---|---|---|
| `--r-card` | `16px` | 卡片、抽屉、大弹层 |
| `--r-ctl` | `12px` | 输入框、下拉、按钮组 |
| `--r-sm` | `8px` | 小标签、单元格 |
| `--r-pill` | `999px` | 胶囊按钮、徽标、计数 |

## 三、阴影（Shadow）

| 令牌 | 值 | 使用 |
|---|---|---|
| `--shadow` | `0 2px 8px rgba(31,45,61,.06)` | 卡片默认 |
| `--shadow-lg` | `0 10px 30px rgba(31,45,61,.14)` | 抽屉、浮层、悬浮 |

## 四、字体（Font）

- 文本栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "MiSans", "Microsoft YaHei", Roboto, sans-serif`
- 数字栈（编号/计数/日期）：`--font-num: "DIN Alternate","Roboto Mono","SFMono-Regular",Consolas,monospace`
- 标题可用楷体点缀（品牌区），正文用系统无衬线，保证跨平台一致。

## 五、间距 / 栅格

- 卡片内边距 `16~20px`；卡片间距 `16px`；主内容区左右安全边距 PC `24px`、移动 `16px`。
- 按钮最小点击区：`44×44px`（移动端）；输入框字号 `≥16px`（避免 iOS 自动放大）。

## 六、中国风纹样（克制）

- 印章式徽标（圆形朱砂边 + 「信」字），置于登录门与顶栏。
- 祥云纹仅作顶栏/页脚点缀，**面积 ≤ 6%**，不抢信息层级。
- 状态用「圆点 + 文字标签」而非纯色块，降低视觉噪音。

## 七、暗色/浅色

当前 v2 为**浅色（宣纸底）**唯一主题。如需暗色，新增 `:root[data-theme="dark"]` 覆盖上述令牌即可，组件代码无需改。
